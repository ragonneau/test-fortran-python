[build-system]
build-backend = "mesonpy"
requires = [
    "meson-python>=0.12.1",
    "wheel",
    "oldest-supported-numpy",
]

[project]
name = "my_project"
description = "My project"
dynamic = ["version"]

